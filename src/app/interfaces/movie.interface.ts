export interface Movie {
  id ?: string,
  src ?: string,
  h_src ?: string,
  name ?: string,
  category ?: string,
  duration ?: string,
  cinema ?: string,
  starring ?: string,
  description ?: string
}
